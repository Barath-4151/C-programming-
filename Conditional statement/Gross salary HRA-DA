#include <stdio.h>
int main() {
    float basic, gross;
    printf("Enter Basic Salary: ");
    scanf("%f",&basic);

    if(basic <= 70000)
        gross = basic + basic*0.30 + basic*0.80;

    printf("Gross Salary = %.2f", gross);
    return 0;
}
