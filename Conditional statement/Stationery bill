#include <stdio.h>
int main() {
    int pen, notebook, pencil;
    int total;

    printf("Enter pen, notebook, pencil quantities: ");
    scanf("%d%d%d",&pen,&notebook,&pencil);

    total = pen*20 + notebook*70 + pencil*9;

    printf("Total Amount = %d", total);

    return 0;
}
